########################
## Cloud Build Config ##
########################

isEnabled: false
docker:
  preset: "npm"
  ignore: "node_modules"
build:
  command: "npm install"
  outputFolder: "dist"
settings:
  isWebsite: true
  firestore: false
  inSecure: true